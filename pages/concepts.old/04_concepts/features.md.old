---
title: Concepts
jscomments:
    active: false
show_sidebar: false
published: true
visible: false
menu: Concepts
content:
    items:
        - '@self.children'
    limit: 20
    order:
        by: default
        dir: asc
    pagination: true
    url_taxonomy_filters: true
feed:
    limit: 10
features:
    - header: Universal Registry of Things
      image: universal-registry.png
      url: universal-registry-things
      text: um texto qualquer
    - header: Universal Registry of Things
      image: universal-registry.png
      url: universal-registry-things
      text: um texto qualquer
    - header: Universal Registry of Things
      image: universal-registry.png
      url: universal-registry-things
      text: um texto qualquer
---

{% set default_collection = page.collection %}

<div class="columns">

{% for p in page.collection %}
  <div class="card" style="width:400px">
  {% if image %}
  <div class="card-image">
      <a href="{{ page.url }}">{{ image.cropZoom(800,400).html|raw }}</a>
  </div>
  {% endif %}
      <div class="card-body">
      <h4 class="card-title">{{ p.title }}</h4>
      <p class="card-text">{{ p.summary }}</p>
      <a href="{{ page.url }}" class="btn btn-primary">Read more</a>
    </div>
  </div>
{% endfor %}

</div>
