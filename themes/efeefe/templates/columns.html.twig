{% extends 'partials/base.html.twig' %}

{% block content %}
    <table>
        <tr>
            {% for column in page.content|split('<hr>') %}
            <td valign=top>{{ column|raw }}</td>
            {% endfor %}
        </tr>
    </table>
{% endblock %}
