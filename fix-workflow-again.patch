<<<<<<< SEARCH
          # Install other dependencies
          # Using || true because grav install returns 1 if .dependencies is missing
          # on a non-skeleton install, which is normal here.
          bin/grav install || true
=======
          # Install other dependencies
          # We need to create an empty .dependencies file to bypass the error
          touch user/.dependencies
          bin/grav install || true
>>>>>>> REPLACE
